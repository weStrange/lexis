/* @flow */

import styled from 'styled-components'

import Popover from 'material-ui/Popover'

const StyledPopover = styled(Popover)`padding: 15px 15px 15px 15px !important;`
export default StyledPopover
